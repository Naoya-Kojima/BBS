<%= form_for(user) do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sex %>
    <%= f.number_field :sex %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>